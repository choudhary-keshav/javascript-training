import styled from 'styled-components';
import { primary } from './colorExports';

export const AppWrapper = styled.div`
  .centerWrapper {
    background-color: ${primary};
    width: 80%;
    margin: 20px auto;
    border-radius: 10px;
    padding: 10px;
  }
`;
