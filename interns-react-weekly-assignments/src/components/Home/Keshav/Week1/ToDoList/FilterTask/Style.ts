import styled from "styled-components";

export const FilterTaskWrapper = styled.div`
    select,
    option {
        background-color: #edeef0;
        padding: 10px;
        border-radius: 10px;
        cursor: pointer;
        margin: 0 0 20px 0;
    }
`